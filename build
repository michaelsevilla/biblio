docker run -i \
  -v `pwd`:/tmp/tex \
  narf/latex \
  /tmp/tex/scale-out.tex
  test.pdf 2> build.log

if [ $? != "0" ] ; then
  echo "ERROR: check build.log for details"
  exit 1
fi

echo "SUCCESS: check out/ folder for output"

exit 0
